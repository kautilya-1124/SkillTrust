# Admin Actions

This folder is reserved for extracting admin POST handlers from page files into dedicated action endpoints.

Current admin pages still process actions inline (with CSRF and auth checks), but this directory is now part of the canonical structure for future cleanup.
